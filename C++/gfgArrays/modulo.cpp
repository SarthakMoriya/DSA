#include<iostream>
#include<string>
using namespace std;

int main(){
    int n=192;
    int m2=n*2;
    int m3=n*3;
    string s=to_string(n)+to_string(m2)+to_string(m3);
    cout<<s;
    for(int i=0;i<s.length();i++)
    {
        if(s[i]=='0' ||)
    }

    return 0;
}